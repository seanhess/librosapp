" Use 'make' to build instead of the default
au filetype typescript setlocal makeprg=make

" Ignore 'Libros'
let NERDTreeIgnore = ['node_modules', '\.xcodeproj', '\.xcworkspace', 'Pods', 'Podfile', 'Libros']
